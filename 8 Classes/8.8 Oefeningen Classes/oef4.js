class Calculate {
    static sum(getal1,getal2){
        return getal1 + getal2;
    }
    static difference(getal1,getal2){
        return getal1 / getal2;
    }
}

let resultaat1 = Calculate.sum(10,5)
let resultaat2 = Calculate.difference(10,5)
console.log(resultaat1 + " " +resultaat2)
